interface:
  display_name: "xc881代码工程规范"
  short_description: "Compact engineering execution"
  default_prompt: "Apply for clear coding. Read→success check→surgical diff→verify. No unrelated cleanup, no single-use abstraction, no routine comments, no implicit commit."
policy:
  allow_implicit_invocation: true
