interface:
  display_name: "xc881代码工程规范"
  short_description: "Boundaries-first engineering execution with compact outputs"
  default_prompt: "Apply xc881-coding-skills: if requirements are unclear, request xc881-requirement-analysis first. Read relevant files before writing, bound the task, use a compact design gate for non-trivial work, make the smallest maintainable change, preserve local style, avoid comments by default, verify before reporting success, and commit/push only when explicitly requested."
policy:
  allow_implicit_invocation: true
