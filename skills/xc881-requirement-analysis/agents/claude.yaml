interface:
  display_name: "xc881需求分析"
  short_description: "Explicit-only requirement handoff"
  default_prompt: "Apply only when explicit. No code. Compact spec, ER/IR, scope, AC, risk, handoff."
policy:
  allow_implicit_invocation: false
