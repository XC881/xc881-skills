interface:
  display_name: "xc881需求分析"
  short_description: "Explicit-only requirement understanding with implicit requirement inference"
  default_prompt: "Apply xc881-requirement-analysis only when explicitly invoked. Do not auto-trigger from normal requirement, PRD, MVP, or product-planning terms. When active, do not write code. Normalize rough or mixed-language requests when needed, separate explicit/inferred/assumed requirements, define scope, acceptance criteria, risks, and a concise engineering handoff."
policy:
  allow_implicit_invocation: false
