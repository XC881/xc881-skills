interface:
  display_name: "xc881需求分析"
  short_description: "Compact requirement analysis with implicit dependency and consequence inference"
  default_prompt: "Apply xc881-requirement-analysis: do not write code. Normalize rough or mixed-language requests when needed, classify new project vs existing feature, separate explicit/inferred/assumed requirements, infer dependencies and consequences, define scope, acceptance criteria, risks, and a concise engineering handoff."
policy:
  allow_implicit_invocation: true
