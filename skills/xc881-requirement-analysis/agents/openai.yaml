interface:
  display_name: "xc881需求分析"
  short_description: "Explicit-only compact requirement analysis"
  default_prompt: "Use $xc881-requirement-analysis only when explicit. No code. Spec→ER/IR→scope→FR/NFR/AC→risk→handoff. One blocking question max."
policy:
  allow_implicit_invocation: false
