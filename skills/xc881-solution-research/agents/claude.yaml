interface:
  display_name: "xc881技术方案联网调研"
  short_description: "Explicit-only compact solution research"
  default_prompt: "Apply only when explicit. No code. Spec, RQ, sources, options, pick, risk, handoff. Prefer primary sources."
policy:
  allow_implicit_invocation: false
