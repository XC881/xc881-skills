# Paper First

Use for algorithms, systems, DB, ML, crypto, consensus, compiler/runtime, security model, performance theory.

Process: survey → core paper → benchmark/repro repo → limitation.

Output:

```text
Paper:
claim:
why:
limit:
```
