interface:
  display_name: "xc881漏洞与Bug诊断"
  short_description: "Explicit-only bug/vuln analysis"
  default_prompt: "Apply only when explicit. No code. Evidence, cause, fix, verify, risk, handoff."
policy:
  allow_implicit_invocation: false
