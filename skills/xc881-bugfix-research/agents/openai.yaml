interface:
  display_name: "xc881漏洞与Bug诊断"
  short_description: "Explicit-only compact bug/vuln diagnosis"
  default_prompt: "Use $xc881-bugfix-research only when explicit. No code/test/commit. Evidence→cause→fix plan→verify plan→handoff. No exploit steps; weak evidence stays uncertain."
policy:
  allow_implicit_invocation: false
