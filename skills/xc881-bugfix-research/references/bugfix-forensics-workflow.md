# Forensics

```text
repro plan → localize → cause → fix plan → verify plan → handoff
```

Evidence: expected, actual, log/stack, env/version, trigger input, recent change, affected module, tests.

Cause = smallest explanation predicting failure.
